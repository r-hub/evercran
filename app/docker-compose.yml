volumes:
  packages:
  dbdata:

services:
  evercran:
    image: "rhub/evercran"
    build:
      context: evercran
    environment:
      - PG_USER=postgres
      - PG_HOST=db
      - PG_PASS_FILE=/db-password
      - PORT=80
    depends_on:
      - db
    ports:
      - "80:80"
    volumes:
      - packages:/packages
    deploy:
      update_config:
        order: start-first
        failure_action: rollback
        delay: 10s
      rollback_config:
        parallelism: 0
        order: stop-first
      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
        window: 120s

  db:
    image: "rhub/evercran-db"
    build:
      context: db
    environment:
      - PGDATA=/var/lib/postgresql/data
      - POSTGRES_PASSWORD_FILE=/db-password
    volumes:
      - dbdata:/var/lib/postgresql
    deploy:
      update_config:
        order: start-first
        failure_action: rollback
        delay: 10s
      rollback_config:
        parallelism: 0
        order: stop-first
      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
        window: 120s

  shell:
    image: "rhub/evercran-db"
    build:
      context: shell
    command: /root/entrypoint.sh
    environment:
      - PG_USER=postgres
      - PG_HOST=db
      - PG_PASS_FILE=/db-password
    volumes:
      - packages:/packages
    deploy:
      restart_policy:
        condition: any
